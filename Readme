# 编译指令
mkdir build && cd build 
cmake .. 
make

# 路径修改
需要在CMakeList指定omp路经

# 运行指令
./raytrace(.exe)

# config配置项
参数输入均在constants.hpp中配置。
主要配置：source_plane_xbox, source_plane_ybox, substrate_xbox, substrate_ybox, process_time
source_plane指定光源位置。光线起始点在该盒子内随机生成。
substrate_xbox指定基底位置。光线与该盒子判定相交。
process_time指定模拟时间。

光源的添加没有自动化。如果需要添加多个光源，需要在raytrance.cpp中添加tracer->AddNewSource(...)语句。

# 输出文件说明
result文件夹下的txt文件保存每一步的结果。由于形貌表示是分片连续的直线段，所以存储的格式为：
    起点x 起点y 终点x 终点y 演化速率（打在该直线上的通量）